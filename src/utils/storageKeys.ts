// Centralized localStorage keys to prevent mismatches across the application
export const STORAGE_KEYS = {
  userId: 'cgf_user_id',
  authToken: 'cgf_auth_token',
  currentUser: 'cgf_current_user',
  userCache: 'cgf_user_cache'
} as const

